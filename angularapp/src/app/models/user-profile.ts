export class UserProfile {
  id: number;
  image_url:string;
  is_private:boolean;
  social_only:number;
  is_blood_donor:boolean;
  is_plasma_donor:boolean;
  covid_recover_date:string;
  blood_group:string;
  phone_no: string;
  show_mobile_number: boolean;
}
