import { DonateDigitFormatPipe } from './donate-digit-format.pipe';

describe('DonateDigitFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DonateDigitFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
